/**
 * Supabase has been removed — this app now uses lib/jsondb.ts (a local db.json
 * with optional GitHub sync). This file is intentionally empty; nothing imports
 * it anymore. Kept only because it could not be deleted on this filesystem.
 */

export {};
